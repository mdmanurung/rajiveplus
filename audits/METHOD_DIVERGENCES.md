# Method divergences

Snapshot: 2026-09-20. Source identity is recorded in
`docs/_codexdocs/execution/2026-09-19/candidate_identity.json`.

| Profile or capability | Relationship to the default | Evidence state |
|---|---|---|
| `RJP-BASE` | Frozen local complete-data default. The completion stream did not change its statistical routing. | Targeted tests pass; final broad receipt is recorded separately. |
| `RJP-COMPAT` | Benchmark-only description of the original RaJIVE path. | `BLOCKED`: the cited upstream clone is absent and network access was not authorized, so no commit was guessed. |
| `RJP-GEOM-CANDIDATE` | Projector-sum aggregation plus candidate geometry, identifiability, Individual, and Wedin contracts. Internal only. | `REJECT`: all three contamination pilot replicates exceeded the frozen 0.02 projector-error degradation margin. |
| Joint coordinates | `get_joint_coordinates()` exposes one score basis and per-block coefficients without changing estimation. | Local contract and reconstruction tests pass. |
| Variance partition | Adds component energy, all cross terms, matrix closure, and energy closure. The legacy `reconstruction_error` meaning is preserved. | Local public-column and reconstruction tests pass. |
| Native missing-data method | Preserves the native algorithm while separating outer, completion, and IRLS controls and metadata. | `EXPERIMENTAL`: 8/8 pilot fits completed, but only 1/8 bounded outer loops converged and no coverage gate ran. |
| Fixed-score jackstraw and pooled PIP | Existing fixed-score approximation retained; a fixed-rank full-refit comparator is available for small data. | `EXPERIMENTAL`: scheduled dataset-level calibration was not run. |
| Robust SVD | Current weighted R and complete-data C++ paths remain executable. No diagonal-algebra optimization was introduced. | Upstream equivalence is `BLOCKED` by the missing source pin. |

Candidate-only helpers are tested directly. They are not called by the default
profile after the paired candidate decision.
