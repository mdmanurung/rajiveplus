% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{summarize_components}
\alias{summarize_components}
\title{Summarize component-level outputs from rajiveplus helper workflows}
\usage{
summarize_components(
  ajive_output = NULL,
  jackstraw_result = NULL,
  assoc_results = NULL,
  stability_result = NULL,
  blocks = NULL,
  summary_type = c("components", "variance", "significance", "associations", "stability",
    "diagnostics"),
  block = NULL,
  component = NULL,
  top_n = NULL,
  ...
)
}
\arguments{
\item{ajive_output}{Optional object of class \code{"rajive"}.}

\item{jackstraw_result}{Optional object of class \code{"jackstraw_rajive"}.}

\item{assoc_results}{Optional association result table from
\code{\link{associate_components}}.}

\item{stability_result}{Optional stability result object from
\code{\link{assess_stability}}.}

\item{blocks}{Optional list of block matrices (needed for
\code{summary_type = "variance"}).}

\item{summary_type}{Character scalar. One of \code{"components"},
\code{"variance"}, \code{"significance"}, \code{"associations"},
\code{"stability"}, or \code{"diagnostics"}.}

\item{block}{Optional integer block index filter.}

\item{component}{Optional integer component index filter.}

\item{top_n}{Optional integer to keep only top rows per summary.}

\item{...}{Reserved for future extensions.}
}
\value{
A \code{data.frame} with stable columns for the requested summary
  type.
}
\description{
Aggregates common outputs into compact tables suitable for reporting and
downstream plotting. Supported summaries include decomposition structure,
variance-explained tables, jackstraw significance counts, metadata
associations, stability outputs, and AJIVE rank diagnostics.
}
\examples{
\donttest{
n <- 40; pks <- c(30, 20)
Y <- ajive.data.sim(K = 2, rankJ = 2, rankA = c(4, 3),
                    n = n, pks = pks, dist.type = 1)
fit <- Rajive(Y$sim_data, c(4, 3))
summarize_components(ajive_output = fit, summary_type = "components")
summarize_components(ajive_output = fit, summary_type = "diagnostics")
}

}
