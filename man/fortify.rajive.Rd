% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{fortify.rajive}
\alias{fortify.rajive}
\alias{fortify.jackstraw_rajive}
\title{Fortify Rajive objects into tidy data frames}
\usage{
\method{fortify}{rajive}(
  model,
  data = NULL,
  what = c("scores", "loadings", "variance", "rank_diagnostics"),
  ...
)

\method{fortify}{jackstraw_rajive}(model, data = NULL, what = "significance", ...)
}
\arguments{
\item{model}{An object of class \code{"rajive"} or
\code{"jackstraw_rajive"}.}

\item{data}{Ignored; present for S3 compatibility.}

\item{what}{Character scalar passed to \code{\link{extract_components}}.
For \code{"rajive"} objects the default is \code{"scores"};
for \code{"jackstraw_rajive"} objects the default is
\code{"significance"}.}

\item{...}{Additional arguments forwarded to \code{\link{extract_components}}.}
}
\value{
A \code{data.frame} in long format.
}
\description{
S3 dispatch for \code{ggplot2::fortify}.  Returns a tidy
\code{data.frame} extracted from the object, suitable for use in
\code{ggplot2} pipelines.
}
\examples{
\donttest{
n <- 40; pks <- c(30, 20)
Y   <- ajive.data.sim(K = 2, rankJ = 2, rankA = c(4, 3),
                      n = n, pks = pks, dist.type = 1)
fit <- Rajive(Y$sim_data, c(4, 3))
head(ggplot2::fortify(fit))

jsr <- jackstraw_rajive(fit, Y$sim_data)
head(ggplot2::fortify(jsr))
}

}
